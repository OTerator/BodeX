#pragma once

class App; // global-namespace application object (see app/App.h)

namespace gt::ui {

// Launcher screen: New / Open buttons and a clickable recent-projects list.
void homeScreen(App& app);

} // namespace gt::ui
