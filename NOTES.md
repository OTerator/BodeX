# BodeX — later / ideas

Parked items to revisit when needed (not scheduled yet):

- **Grades export** — CSV now, then match the Excel format the user will provide.
  Add an in-app **File → Export…** entry (wire it disabled until the format is
  decided) that writes per-student totals + per-question scores.
- **Compact cell text eliding** — a very long "last page" value can still be
  clipped in the grid cell; consider middle-elide or a wider cell / tooltip.
  (Two-line cells already give the last page its own line.)
- Optional: per-question column show/hide, and CSV/Excel *import* of a roster.
